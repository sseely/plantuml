# Plan Mission Progress — dispatch-by-parse-attempt

## Phase 1: done
Stack: TypeScript, vitest (`npm test`, coverage 90/90/90), `npm run typecheck`
(two tsconfigs), `npm run lint` (eslint), `npm run build` (vite).
Arch overview: docs/architecture/overview.md (pipeline parse -> layout -> render;
registry.resolve() picks plugin via accepts()).
Catalog: docs/catalog.md, `npm run catalog`, drift-gated.

## Phase 2: confirmed
Blast radius presented and confirmed 2026-08-24. See brief README once written.
Upstream refusal contract (PSystemCommandFactory.java): getCandidate==null ->
SYNTAX_ERROR (:169-175); execute fail -> EXECUTION_ERROR w/ score (:180-186);
isIncomplete -> null (:159-161); checkFinalError (:148-152). isOk at
PSystemBuilder.java:296. All-fail tie-break = mergeV2 max score
(PSystemErrorUtils.java:140-147).
Candidate set: DiagramType.findStartTypes (DiagramType.java:69-92); only
@startuml is multi (:198-201). SCOPE CORRECTION: parse-attempt discriminates
among 9 upstream factories, 5 ported (sequence, class, activity, description,
state) -- NOT the ">=13 engines" in planning/next-missions.md:611-620.
Factory bases: sequence/class/activity3/description/state/board/chart/packet =
PSystemCommandFactory (strict command loop). json/yaml/hcl/files =
PSystemAbstractFactory (own document parsers). dot = PSystemBasicFactory.
File table: dispatcher.ts, block-extractor.ts#detectUmlType, 5 parsers,
14 */index.ts accepts(), src/index.ts order, descriptive-keywords.ts (527) +
class-dispatch.ts (451) likely deleted, routing-conformance.test.ts = SLI
(2 real misroutes / 8 jar-error / 3158 fixtures).

## Phase 2b: user rulings (2026-08-24)
- REFUSAL SCOPE = **full faithful refusal**. resolve() and render() share one
  strict parse; unmatched line -> error diagram; all-candidates-fail ->
  mergeV2 max score. Corpus-wide error-page and golden/ratchet movement is
  accepted and expected.
- ORDER FREEZE = **same batch, gated**. Refusal + src/index.ts order re-mirror
  land together, routing gate as the boundary check. Neither commits alone.

## Phase 3: pending
